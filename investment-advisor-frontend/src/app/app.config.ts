export const AppConfig = {
    apiUrl: 'http://127.0.0.1:8000/api', // Base URL for the FastAPI backend
    timeout: 5000, // Request timeout in milliseconds
    // Add other configuration settings as needed
};